You can use docker-compose to have multiple containers running in the same network.

Instructions:

1. Build the containers:
`docker-compose build --no-cache`

2. Run the containers:
`docker-compose run -d`
